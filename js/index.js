function imageSwap() {
    if (teamID.value == "CMDblues") {
        document.getElementById("teamBadge").src="graphics/CMDblues.png";
    }
    else if (teamID.value == "CMDreds") {
        document.getElementById("teamBadge").src="graphics/CMDreds.png";
    }
}
